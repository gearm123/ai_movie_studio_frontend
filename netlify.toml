# Same pattern as translate-chat-frontend (direct API URL, no proxy).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE_URL = "https://ai-history-api.onrender.com"

# Render backend: CORS_ORIGINS=https://gearmstudio.netlify.app
# Netlify: VITE_BACKEND_API_KEY = same value as Render API_KEY

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
